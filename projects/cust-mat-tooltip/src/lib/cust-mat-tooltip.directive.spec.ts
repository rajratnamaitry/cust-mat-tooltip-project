import { CustMatTooltipDirective } from './cust-mat-tooltip.directive';

describe('CustMatTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CustMatTooltipDirective();
    expect(directive).toBeTruthy();
  });


});
